#include <iostream>

int main(int , char** )
{
    std::cout << "size of unsigned long is " << sizeof(unsigned long) << "." << std::endl;
    std::cout << "size of uint32_t is " << sizeof(uint32_t) << "." << std::endl;
    return(0);
}
